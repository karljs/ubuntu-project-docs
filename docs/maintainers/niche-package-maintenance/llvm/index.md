(clang-llvm-packaging)=
# `llvm` and `clang` packaging

Guidance on how to package and maintain versions of `llvm` and
`clang`.

```{toctree}
:maxdepth: 1

LLVM Packaging guide <packaging>
How to bootstrap LLVM <bootstrapping>
```
